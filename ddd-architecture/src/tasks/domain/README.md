# Tasks Domain

- Modela tareas como Aggregates con reglas (`rename`, `complete`, `reopen`).
- Value Objects capturan significados (p. ej. `TaskTitle`).
- Puertos declaran acceso y políticas de consistencia del conjunto.

