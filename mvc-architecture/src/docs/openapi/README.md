# OpenAPI Docs

- Registro de endpoints con `zod-to-openapi`.
- Generación de documento con `scripts/generate-openapi.ts`.
- Servido por Swagger UI en `/docs`.
- Facilita pruebas y contratos entre equipos.
