# Routes

- Definen endpoints y asignan controladores.
- Componen middlewares (`auth`, `validate`, `error`).
- Estructuran rutas REST para `users`, `tasks`, `auth`.
- No contienen lógica de negocio.
