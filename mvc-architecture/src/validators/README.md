# Validators

- Esquemas Zod para `body`, `params`, `query`.
- Garantizan contratos de entrada consistentes.
- Integran con OpenAPI para documentar endpoints.
- Usados por `validate.middleware.ts` en rutas.
